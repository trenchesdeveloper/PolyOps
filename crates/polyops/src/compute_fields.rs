//! Computation of in/out and edge-type flags for sweep events,
//! port of upstream `src/compute_fields.ts`.

#![allow(dead_code)]

/// Compute the `inOut`, `otherInOut`, and `edgeType` fields on a sweep
/// event based on its predecessor on the sweep line.
///
/// TODO: port from upstream `src/compute_fields.ts`.
pub(crate) fn compute_fields() {
    todo!("port src/compute_fields.ts")
}
